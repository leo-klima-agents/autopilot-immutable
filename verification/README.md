# Reproducibility & verification (release gate, brief §8)

**Nothing is seeded until every item below is publicly checkable.**

## Pinned toolchain

| Item | Value |
|---|---|
| solc | `0.8.30+commit.73712a01` (exact patch pinned in `foundry.toml`) |
| optimizer | enabled, `runs = 200` |
| via_ir | `false` |
| evm_version | `cancun` |
| bytecode_hash | `ipfs` (metadata recorded, appended to runtime code) |
| external dependencies | none — `libs = []`, `remappings = []` |

## Deterministic deployment (EpochPilot, series 1)

| Item | Value |
|---|---|
| CREATE2 factory | `0x4e59b44847b379578588920cA78FbF26c0B4956C` (canonical deterministic-deployment proxy) |
| salt | `keccak256("tranche-pilot/EpochPilot/series-1")` = `0x2f4ab442c035d1a9c8b5950925f27d098fe4ed5b40ce69733f5c794ee2d39481` |
| constructor arg | Voter, resolved on-chain at deploy time (see `addresses.md`); `0x16613524e02ad97eDfeF371bC883F2F5d6C480A5` at the recorded block |
| init code hash | `0xcc9065d7bc8ff98b7c495374c55e44988158aa5684188c894d0c28feb88e70d7` |
| **derived address** | `0x2883C715e5f8c1edE4b95d96F2ADFA2E70aBc654` |
| runtime bytecode keccak256 | `0x47f9f1036eb0767300f49be1ff2986c0b3fa313eca9161793fcbb98124b223ac` |

Re-derive the address yourself:

```
cast create2 \
  --deployer 0x4e59b44847b379578588920cA78FbF26c0B4956C \
  --salt $(cast keccak "tranche-pilot/EpochPilot/series-1") \
  --init-code-hash <keccak256(creation_bytecode ++ abi.encode(voter))>
```

## One-command local reproduction

From a clean clone (no lockfiles, no network state — solc is fetched by exact
version pin):

```
forge build
cast keccak "$(python3 -c "import json;print(json.load(open('out/EpochPilot.sol/EpochPilot.json'))['deployedBytecode']['object'])")"
# → must equal the runtime bytecode hash above
```

Against the deployed contract (post-deployment):

```
diff <(cast code 0x2883C715e5f8c1edE4b95d96F2ADFA2E70aBc654 --rpc-url $BASE_RPC_URL) \
     <(python3 -c "import json;print(json.load(open('out/EpochPilot.sol/EpochPilot.json'))['deployedBytecode']['object'])")
```

(Byte-identical including metadata, because the toolchain is fully pinned and
`bytecode_hash = "ipfs"` is recorded here rather than stripped.)

## Verification order (must both be live before seeding)

1. **Sourcify exact-match** (keyless): submit `EpochPilot.standard-input.json`
   + metadata; expect a *full match* (metadata hash equality), not a partial match.
2. **Basescan**: upload the same `EpochPilot.standard-input.json`
   (generated by `forge verify-contract --show-standard-json-input`, committed
   in this directory) so the code is readable where Base users actually look.
3. CI's `repro` job rebuilds from a clean checkout on every commit and compares
   hashes; after deployment it compares against `cast code` of the live address.

## Deployment status

- [ ] Base Sepolia rehearsal
- [ ] Base mainnet deploy (requires a funded broadcast key — deliberately not
      held by this repo's automation)
- [ ] Sourcify exact-match live
- [ ] Basescan verified
- [ ] bytecode-diff CI green against deployed address
- [ ] seeding opened
